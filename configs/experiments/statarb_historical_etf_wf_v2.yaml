# Stat-Arb historical walk-forward study — INVALIDATED BEFORE HOLDOUT
# Status: invalidated-before-holdout (was: frozen-for-holdout)
#
# CORRECTIVE NOTE (independent audit, see quant-research-portfolio Issue #3):
# This freeze was procedurally invalid. The branch never contained a
# manifest for dataset_id below, a conforming executable pipeline, or
# reproducible DEV/2024-validation artifacts demonstrating this config was
# actually run -- only this YAML document existed. No v2 2025 result
# artifact was ever generated, and none was inspected.
#   original_freeze_timestamp_utc: "2026-09-16T13:37:28Z"
#   invalidation_timestamp_utc:    "2026-09-16T17:30:00Z"
#   reason: "No dataset manifest, no pipeline code, no DEV/VAL artifacts existed
#            for this config anywhere in the repository at the time of freeze."
#   no_conforming_2025_v2_holdout_executed: true
#   no_v2_2025_result_artifact_exists: true
#   superseded_by: statarb_hist_etf_wf_v3 (configs/experiments/statarb_historical_etf_wf_v3.yaml)
#
# This file is preserved unmodified below (git history + this header are the
# audit trail) -- do not delete or rewrite history.
#
# Prior exploratory config statarb_historical_oos_study_v1.yaml remains
# SUPERSEDED / EXPLORATORY / NON-CONFORMING as before.

experiment_id: statarb_hist_etf_wf_v2
status: invalidated-before-holdout
directive: 9
dataset_id: yf_stat_arb_etfs_daily_2015_2025_v1
seed: 0

universe:
  source: Directive_9_D9B_authoritative_ETF_groups_A_D
  interval: 1d
  pair_constraint: within_group_only
  groups:
    GROUP_A_BROAD_EQUITY: [SPY, QQQ, DIA, IWM]
    GROUP_B_US_EQUITY_SECTORS: [XLB, XLE, XLF, XLI, XLK, XLP, XLRE, XLU, XLV, XLY]
    GROUP_C_TREASURY_ETFS: [SHY, IEF, TLT]
    GROUP_D_PRECIOUS_METALS: [GLD, SLV]

periods:
  requested_data_start: "2015-01-01"
  requested_data_end_exclusive: "2026-01-01"
  formation_dev: {start: "2015-01-02", end_inclusive: "2023-12-29"}
  validation: {start: "2024-01-02", end_inclusive: "2024-12-31"}
  holdout: {start: "2025-01-02", end_inclusive: "2025-12-31"}
  holdout_access: allowed_after_final_configuration_frozen

selection:
  method: engle_granger_within_group
  fdr_alpha: 0.05
  adf_alpha: 0.05
  multiple_testing: Benjamini-Hochberg on Engle-Granger p-values (pooled within-group candidates)
  secondary_filter: residual_ADF
  max_pairs_traded: 1
  ranking: lowest_BH_adjusted_EG_p then lower_residual_ADF_p then lexicographic_symbols

signals:
  entry_z_candidates: [1.5, 2.0, 2.5]
  exit_abs_z_candidates: [0.25, 0.50, 0.75]
  trailing_z_window_candidates: [20, 40, 60]
  same_timestamp_batch_required: true

hedge:
  baseline: static_ols
  alternative: sequential_kalman_forward_filter_only
  fair_compare: same_pair_oos_entry_exit_costs_sizing

kalman:
  process_variance_candidates: [1.0e-5, 1.0e-4, 1.0e-3]
  observation_variance: 1.0e-2
  observation_variance_note: repo-validated default; document any scale fix BEFORE final
  alpha_random_walk: true
  no_smoothing: true

dev_default_params:
  entry_z: 2.0
  exit_abs_z: 0.5
  trailing_z_window: 40
  kalman_process_variance: 1.0e-4
  kalman_observation_variance: 1.0e-2

selection_objective:
  primary: net_sharpe
  min_trade_count: 10
  tie_break: [lower_max_drawdown, lower_turnover]
  insufficient_trades_fallback:
    entry_z: 2.0
    exit_abs_z: 0.5
    trailing_z_window: 40

sizing:
  mode: fixed_gross_notional
  gross_notional_multiple_of_allocated_nav: 1.0
  max_leverage_gross: 1.0

costs:
  scenarios: [analytical_zero, BASE, STRESS]
  BASE:
    commission_per_share: 0.005
    min_commission: 1.0
    spread_bps_per_side: 1.0
    slip_bps_per_side: 1.0
    impact_bps_per_side: 0.5
    borrow_bps_annualized: 50.0
  STRESS:
    commission_per_share: 0.005
    min_commission: 1.0
    spread_bps_per_side: 3.0
    slip_bps_per_side: 3.0
    impact_bps_per_side: 2.0
    borrow_bps_annualized: 200.0
  analytical_zero:
    commission_per_share: 0.0
    min_commission: 0.0
    spread_bps_per_side: 0.0
    slip_bps_per_side: 0.0
    impact_bps_per_side: 0.0
    borrow_bps_annualized: 0.0
  notes: >
    Borrow applied as annualized_bps/252 per day on short market value
    (economically equivalent to engine borrow_bps_per_day).

walk_forward:
  formation_size: 504
  validation_size: 126
  test_size: 63
  step_size: 63
  reject_under_length_windows: true
  no_future_filling: true

evaluation:
  invent_metrics: false
  report_nulls_exactly: true

constraints:
  no_2025_evaluation_until_freeze: false
  no_retune_after_freeze: true
  no_universe_change_after_dataset_freeze: true
  no_ci_network_fetch: true

frozen_params:
  static_ols:
    entry_z: 2.0
    exit_abs_z: 0.5
    trailing_z_window: 40
    kalman_process_variance: null
    kalman_observation_variance: 1.0e-2
  kalman:
    entry_z: 2.0
    exit_abs_z: 0.5
    trailing_z_window: 40
    kalman_process_variance: 1.0e-4
    kalman_observation_variance: 1.0e-2
  selection_basis: >
    2024 VAL windows (n=3) produced 0 eligible pairs under BH FDR 0.05 + residual ADF 0.05
    for both hedge models; min trade count >=10 not met. Retained canonical baseline
    entry 2.0 / exit 0.5 / z-window 40 (Kalman process variance 1e-4; observation variance
    repo default 1e-2; no scale fix). Primary objective net Sharpe unused due to null trades.
  frozen_for_holdout_utc: "2026-09-16T13:37:28Z"

notes: >
  Conforming D9-B experiment. Prior PR #7 exploratory SECTOR_PAIRS equities
  study (statarb_hist_oos_v1_*) is SUPERSEDED / non-conforming — retained in
  ledger with explicit notes; new experiment IDs under this config only.
